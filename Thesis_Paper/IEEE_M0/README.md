# IEEE M0 paper

`main.tex` is the publication-style draft for **frozen script-aware BM25 (M0)** only.

It is **not** the MiniLM dual-index routing paper in `../IEEE/main.tex`. Do not merge P@5 tables from that file into this one.

Compile with a standard IEEEtran conference setup. No new experiments were run for this draft.
