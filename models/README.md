# models/

Official freeze dictionary: **`roman_urdu_dict_expanded.json`** (198 keys).

Layer A SVM pickles and backups were moved to `archive/historical_experiments/models_svm_layer_a/` and `models_svm_backups/`. They are not the official retriever.
