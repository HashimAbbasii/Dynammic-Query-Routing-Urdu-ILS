# Phase 9 — Held-out evaluation (one shot)

Executed after Phase 8 freeze. See `PHASE9_RESULTS.md`.

Preflight: `artifacts/preflight.json`  
Per-query: `HELD_OUT_PER_QUERY.csv`  
Metrics: `artifacts/official_metrics.json`

Do not rerun to chase a number. Do not invent `source_doc_id`.
