# Thesis figures

The final DOCX embeds **21 PNG images** in `word/media/` (`image1.png` … `image21.png`). Document relationships use `r:embed` only; `r:link` count is 0 (no external image files).

Images were **not** extracted or replaced. Opening the DOCX is sufficient.
